# Discord bot token — create a bot at https://discord.com/developers/applications
DISCORD_BOT_TOKEN=your-bot-token-here

# Optional: default guild (server) ID used when guild_id is not passed to a tool
DISCORD_GUILD_ID=

# Optional: how many incoming messages to keep in memory (default: 100)
MESSAGE_CACHE_SIZE=100
